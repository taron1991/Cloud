server:
  port: 0

spring:
  application:
    name: eClient2

eureka:
  client:
    service-url:
      defaultZone:
        http://localhost:8081/eureka
    register-with-eureka: true
    fetch-registry: true
